name: mobile_app
description: "A new Flutter project."

publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  screenshot: ^3.0.0     
  image: ^4.3.0          
  esc_pos_utils_plus: ^2.0.3
  flutter_bluetooth_printer: ^2.20.0
  another_brother: ^2.0.0  # สำหรับสั่งงานเครื่องพิมพ์ Brother
  permission_handler: ^12.0.1 # สำหรับขออนุญาต Bluetooth
  path_provider: ^2.0.0    # สำหรับจัดการไฟล์รูปภาพ (ถ้าจำเป็น)
  o3d: ^3.1.0
  
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  mobile_scanner: ^7.1.4  
  google_fonts: ^6.1.0
  audioplayers: ^6.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  
  flutter_lints: ^6.0.0


flutter:

  uses-material-design: true
  assets:
    - assets/aj.glb
    - assets/welcome.mp3

